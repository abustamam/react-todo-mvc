{
  "name": "todo-mvc-react",
  "version": "1.0.0",
  "description": "Todo MVC for react course",
  "main": "index.js",
  "scripts": {
    "build": "./build.sh",
    "dev": "PORT=4000 node ./webserver/index.js",
    "prod": "node ./webserver/index.js",
    "postinstall": "./install.sh && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "8.4.0"
  }
}
